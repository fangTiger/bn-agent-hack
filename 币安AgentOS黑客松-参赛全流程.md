# 币安 Agent OS Mini Hackathon · 参赛全流程

> 这份是走完一遍之后回头写的。每一个「坑」都是真踩过、卡住过的，不是从文档抄来的注意事项。
> 照着做能省掉大半天的排查时间。

| | |
|---|---|
| 总奖池 | **60,000 USDC** |
| Track B | 40,000 USDC（极简，半小时能完成） |
| Track A | 20,000 USDC（评审制，需要作品） |
| 截止 | **2026-09-08 23:59 UTC** = 北京时间 09-09 07:59 |

两个 Track **可以同时投**，互不影响。Track B 是低成本彩票，Track A 才是期望值来源 —— 但**先做完 B，再全力做 A**。

---

## 00 · 先花两分钟确认资格

**这是一票否决项。不合格的话，后面所有工作都是白做。**

币安同类活动通常**排除美国、英国、EEA 成员国、香港、新加坡**，并要求完成身份验证（KYC）。本次活动的具体条款请在官方页面确认 —— **先确认，再动手**。

> **为什么放在第一步**
> 我们直到中途才去核这件事。如果当时发现不合格，前面所有投入就全没了。
> 两分钟能排除的风险，不要留到最后。

---

## 01 · Track B —— 先把这 40K 的份额锁上

要求极简，半小时能完成，没有质量评分。**官方原文只有两句：**

> Connect your MCP to your agent and complete a Spot trade using your MCP-connected agent.
> Please provide your UID

### 步骤 1：装 Claude Code，注册 Binance MCP

```bash
claude mcp add binance-mcp-server --transport http https://agent.binance.com/mcp/agentic
```

然后在 Claude Code 里输入 `/mcp`，完成 OAuth 授权，勾选 **Spot** 权限。

> ### ⚠️ 坑 1 · 加完 MCP 看不到它
> 会话中途 `claude mcp add` 加的 server **不会**出现在当前会话的 `/mcp` 菜单里。
> 必须**在同一个目录下重启 Claude Code** 才能看到。我们在这里卡了很久。

> ### ⚠️ 坑 2 · 授权不继承到子目录
> MCP 是以 local scope 绑定在**你执行 `claude mcp add` 的那个目录**上的。
> 在它的子目录里跑 `claude -p` 会得到「binance MCP 工具当前不可用」，而且不解释原因。
> 要么就在你将来要工作的目录里注册，要么运行时先 `cd` 回授权目录。

### 步骤 2：建 Agentic 子账户并转钱进去

Agent 的 `enableWithdrawals` 是 **false**，它**无法自己把钱转进来**，这第一笔必须你手动做：

```
https://www.binance.com/en/my/sub-account/asset-management/transfer
```

转 **USDT** 到 **现货（Spot）** 钱包。20 USDT 就够 Track B 用了。

> ### ⚠️ 坑 3 · 转错币种会多绕一圈
> 如果你转的是 USDC，注意**代币化美股只有 USDT 交易对**（`TSLABUSDC` 不存在）。
> Agent 自己能换（`convert_sendQuoteRequest` → `acceptQuote`），
> 实测 USDC→USDT 汇率 0.999919，损耗 0.008%，比走现货挂单的 0.1% 手续费划算得多。

### 步骤 3：让 agent 经 MCP 下一笔现货单

直接跟 Claude 说「用 binance MCP 买 10 USDT 的 BTCUSDT 市价单」即可。

**这一步必须由 agent 通过 MCP 完成**，不能自己在 App 里手点 —— 那是官方唯一能验证的东西。

> ### ⚠️ 坑 4 · 最小名义额 5 USDT，市价单也算
> `minNotional = 5 USDT`，且 `applyMinToMarket = true`。
> 低于这个数会被拒：`-1013 Filter failure: NOTIONAL`。
>
> 另外数量会被 `stepSize` **向下取整**，所以下单 10 USDT 实际可能只花 9.27，不影响达标。

### 步骤 4：填表 + 社交动作

Follow @Binance、转发活动帖、填 survey。

**UID 那栏填主账户 UID，只填一个纯数字。**

**为什么是主账户？** 表单原文是 `Please provide your UID` —— 「你的 UID」，指参赛者本人，不是「执行交易的那个」。三条理由：

1. 措辞指向个人账户
2. 多数人都会填主账户，所以官方的验证脚本必然做了父子账户 join
3. **奖金只能发到能提现的账户**，而子账户提现是关闭的

但**不要在那一栏里塞两个 UID**。自动化校验大概率是精确匹配，塞两个会从「可能有风险」变成「确定失败」。想对冲就写在 X 帖子的自由文本里。

---

## 02 · Track A —— 20K 的评审制赛道

表单五个必填字段：

| # | 字段 |
|---|---|
| 1 | **主题四选一**：Data Analysis / Trading Workflows / Payment Workflows / Onchain Workflows |
| 2 | **项目文字描述** |
| 3 | **视频平台**（YouTube / X / Facebook / Instagram / Others） |
| 4 | **公开视频链接** |
| 5 | **step-by-step 复现指南** |

> **注意：没有 GitHub 字段。**
> 复现指南是**直接粘贴进表单的文本**，不是一个链接，所以它必须自包含。
> GitHub 链接只能塞进「文字描述」里当补充材料。我们一开始以为要交仓库，方向差点搞错。

### 怎么写才不被淹没

- **文字描述在视频链接之前** —— 它决定评委要不要点开你的视频，是唯一保证会被读到的东西。前两句必须能独立成立。

- **放可核验的锚点**：真实订单号、UID、精确到小数位的成交额。币安内部能查。大多数人不会给出可被核实的具体数字，这是很便宜的可信度。

- **复现指南是性价比最高的一项**。表单专门问了它，说明主办方在意；而多数人会在截止前草草写几行。把你踩过的坑写进去 —— 只有真跑过的人写得出那些东西，它比任何架构图都更能证明项目是真的。

- **视频不用做 PPT**。真实终端录屏 + 真实下单过程，比精美动画有说服力得多。2–3 分钟足够。

### 关于主题选择

四个主题的竞争密度和评分口径不一样。选之前先想清楚：**你最硬的证据是什么？**

如果是真实成交记录，就别放进 Data Analysis —— 那会被按「报告质量」评分，订单记录白瞎了。反过来，如果你的强项是数据洞察，硬塞进 Trading Workflows 也讨不到好。

> **建议：别照抄别人的题目。**
> 评审制下雷同作品互相稀释，评委也一眼看得出。
> 真正可复用的是上面这些流程和坑，创意应该是你自己的。

---

## 03 · 如果你要做自动化（Track A 常见需求）

让 agent 定时自己跑起来，这几个坑会让它**静默失败** —— 不报错，就是什么都不做。

先用这条命令模拟 cron 的空环境，能一次性复现全部问题：

```bash
env -i HOME=$HOME /bin/bash your_script.sh
```

> ### ⚠️ 坑 5 · PATH 极简
> cron 环境找不到 `claude` 和 `python3`。脚本里显式 `export PATH`。

> ### ⚠️ 坑 6 · claude 可能是个带代理的 shell alias
> 很多人的 `~/.zshrc` 里把 `claude` 定义成带 http_proxy 前缀的 alias。
> cron 不读 zshrc，于是真实二进制在没有代理的情况下跑，连不上 API。
> 用绝对路径调用，并显式导出代理变量。

> ### ⚠️ 坑 7 · 缺 USER/LOGNAME 会报「Not logged in」
> **这个最阴险**：它报的是**认证错误**，不是环境错误，会把你骗去反复重跑 `/login`。
> 真实原因是 Claude 的凭据存在 macOS Keychain 里，读取它需要用户身份变量。
> 脚本里 `export USER` 和 `LOGNAME` 即可。

> ### ⚠️ 坑 8 · `crontab <文件路径>` 会截断长路径
> 我们的路径最后一个字符被吃掉了，报「No such file」。改用标准输入写入：
> ```bash
> crontab - <<EOF
> */15 * * * * /abs/path/script.sh >> /abs/path/log 2>&1
> EOF
> ```

**装上不等于会跑。** 临时把频率改成 `* * * * *`，等两分钟看日志确认 cron 守护进程真的在执行，再改回正常频率。

---

## 04 · 时间线建议

| 阶段 | 做什么 | 要点 |
|---|---|---|
| D0 上午 | 确认资格 + 打通 MCP + Track B 全部做完 | 先把 40K 的份额锁上，心里踏实 |
| D0 下午 | 定 Track A 的题，**并验证技术可行性** | 见下方 |
| D1–D2 | 实现 + 真实跑起来 | 留出真实运行的时间窗口 |
| D3 | 写文字描述 + 复现指南 | 这两样别留到最后 |
| D4 | 录视频 | 先排练，正式录时数字要看得清 |
| D5 | 提交 | 留一整天缓冲 |

> ### 最重要的一条经验
> **第一天就要验证你方案里最关键的那个技术假设能不能成立。**
>
> 我们做的第一件事是：拿 5 USDT 真实下一单，确认某种订单类型能被接受，然后撤单。
> 如果那时发现下不了，整个设计就得推翻 —— 而那天推翻还来得及，第四天推翻就完了。
>
> 同理，如果你要做定时任务，**第一天就把 cron 空环境跑通**，
> 别等到无人值守的那晚才发现它一直在静默失败。

---

## 05 · 提交前对一遍

- [ ] 地区资格与 KYC 已确认
- [ ] Track B：交易由 agent 经 MCP 完成，UID 填的是**主账户**纯数字
- [ ] Track A 五个字段齐全，**视频链接不是占位符**
- [ ] 视频设为**公开**，不是「不公开列出」
- [ ] 如果放了 GitHub 链接，确认仓库是 **public**，且没把私密内容推上去（账户快照、API 凭据、内部策略文档）
- [ ] 复现指南粘进表单后确认没被截断
- [ ] 材料里的每个数字都能被复现或核验 —— **任何一个站不住的数字都会拖累全部可信度**

---

*祝顺利。有问题可以直接问。*
